{
  "file": "codetwine/pipeline.py",
  "sections": [
    {
      "id": "design",
      "title": "Design Specification",
      "content": "**Overview**\n\nOrchestrates the end-to-end analysis pipeline that turns a project's source tree into dependency data, design documents, and consolidated knowledge artifacts.\n\n- A CLI entrypoint or caller needing full-project analysis calls `process_all_files` to get per-file dependency JSON, LLM-generated design docs, a dependency summary, a Mermaid diagram, and a consolidated JSON/SQLite knowledge base.\n- Internal pipeline steps use `_convert_dep_list_to_internal_paths` to translate the \"project_name/copy_path\" format produced by dependency-graph extraction back into plain relative paths for local processing.\n- `_process_file_dependencies` is invoked once per pipeline run to extract and persist per-file dependency info (`file_dependencies.json`) and copy source files into the output tree.\n- `_detect_changed_files` is used before document generation to decide which files need their `doc.json` regenerated based on source hash comparison.\n\nThis file depends on `build_project_dependencies` (project-wide dependency graph), `get_file_dependencies` and `detect_source_roots` (per-file dependency/import resolution), `compute_file_hash`, `copy_path_to_rel`, `resolve_file_output_dir` (path/hash utilities), `load_doc` and `generate_all_docs` (design-document lifecycle), `save_consolidated_json`, `save_dependency_summary`, `save_dependency_graph_as_mermaid`, `build_symbol_level_deps`, `build_summary_map`, `to_output_path` (knowledge/graph output), `save_consolidated_sqlite` (SQLite export), `LLMClient` (LLM access), `parse_cache` (cache cleanup), and `KNOWLEDGE_FORMAT`/`KNOWLEDGE_FORMATS`/`ENABLE_LLM_DOC`/`MAX_WORKERS` (configuration). It is used by `main.py`, which constructs an `LLMClient` when `ENABLE_LLM_DOC` is set and calls `process_all_files` as the sole pipeline entry point.\n\nDesign notes: `KNOWLEDGE_FORMAT` is validated up front before any analysis work runs, so misconfiguration fails fast. Dependency extraction (Step 2) always runs for all files for consistency, while design-document generation (Step 3) is incremental, driven by source-hash comparison against previously saved `doc.json` files, skipping LLM calls for unchanged files when `ENABLE_LLM_DOC` is true. Per-file failures in dependency extraction are caught and logged individually so one file's failure does not abort the whole run. Empty files are detected and excluded from all downstream processing steps. The tree-sitter `parse_cache` is cleared at the end of the run to free memory.\n\n**Definitions**\n\n## `_convert_dep_list_to_internal_paths`\n\nStrips the `\"{project_name}/\"` prefix and reverses the copy-path directory structure (via `copy_path_to_rel`) on the `file`, `callers`, and `callees` entries of a raw project dependency list, converting the output-facing path format from `build_project_dependencies` into plain project-relative paths used internally by the rest of the pipeline. Called once, right after building the project dependency graph, before any per-file processing begins.\n\n## `_detect_changed_files`\n\nCompares each file's current SHA256 hash (via `compute_file_hash`) against the `source_hash` field recorded in that file's existing `doc.json` (via `load_doc` and `resolve_file_output_dir`) to determine which files have changed since the last run. Returns the set of files needing document regeneration; a missing or unreadable `doc.json` also counts as changed. Used only when `ENABLE_LLM_DOC` is enabled, to drive incremental document generation via `generate_all_docs`.\n\n## `_process_file_dependencies`\n\nRuns `get_file_dependencies` for every file in the given list, converts the resulting `file`/`callee_usages[].from`/`caller_usages[].file` paths to output format via `to_output_path`, writes each file's `file_dependencies.json`, and copies the original source file into its resolved output directory. Builds the shared `project_file_set`, `source_root_set` (via `detect_source_roots`), and `caller_map` lookups once and reuses them across all files for efficiency. Catches and logs per-file exceptions individually so a single failure does not halt processing of the remaining files.\n\n## `process_all_files`\n\nRuns the complete analysis pipeline for a project: validates `KNOWLEDGE_FORMAT`, builds the project-wide dependency graph via `build_project_dependencies`, filters out empty files, extracts per-file dependencies via `_process_file_dependencies`, optionally generates LLM-based design documents via `generate_all_docs` (using `_detect_changed_files` for incremental regeneration when `ENABLE_LLM_DOC` is true), builds symbol-level dependencies (`build_symbol_level_deps`) and summaries (`build_summary_map`), and writes `project_dependency_summary.json`, `dependency_graph.md`, and—depending on `KNOWLEDGE_FORMAT`—`project_knowledge.json` and/or `project_knowledge.sqlite`. This is the single public entry point called by `main.py` to perform a full project analysis; raises `ValueError` immediately if `KNOWLEDGE_FORMAT` is not a recognized value, before any file processing occurs."
    }
  ],
  "summary": "Orchestrates the full source-to-knowledge analysis pipeline: builds project dependency graphs, extracts per-file dependencies, generates incremental LLM design docs, and writes consolidated summaries, Mermaid diagrams, and JSON/SQLite knowledge bases. Main public entry point is process_all_files, supported by helpers _convert_dep_list_to_internal_paths, _detect_changed_files, and _process_file_dependencies. Key terms: dependency graph, design documents, incremental regeneration via source hash, knowledge format validation, consolidated JSON/SQLite output, per-file error isolation, empty-file filtering, and cache cleanup. Used by main.py as the sole pipeline entry point.",
  "source_hash": "e40cc410968a4d331cd6967a1b5c30d51f1f4f10eb2ef9020b655f027077a8e1"
}